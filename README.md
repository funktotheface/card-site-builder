# card-site-builder
A simple alternative to traditional websites, built with node and react

install react with vite and start dev in client dir, install node and run index.js in server dir, change type in package,json to module.

set up an api route using vite proxy so server can work in codespaces, also gonna make it wasier to deploy!